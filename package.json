{
  "name": "fileupload",
  "version": "1.0.0",
  "main": "server.js",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.19.0",
    "config": "^3.2.2",
    "dotenv": "^8.1.0",
    "express": "^4.17.1",
    "fs-extra": "^8.1.0",
    "multer": "^1.4.2",
    "pg": "^7.12.1",
    "pg-format": "^1.0.4"
  },
  "scripts": {
    "start": "node server.js",
    "test": "NODE_ENV=test nyc --reporter=text --reporter=lcov --check-coverage --branches 100 --statements 100 --lines 100 mocha --timeout 10000 ./test/*.spec.js"
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "mocha": "^6.2.0",
    "nyc": "^14.1.1"
  }
}
